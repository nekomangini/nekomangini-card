export interface SocialType {
  platform: string;
  icon: string;
  url: string;
  label: string;
  svgPath: string;
}

export interface ActionType {
  icon: string;
  label: string;
  url: string;
  svgPath: string;
}

export interface ProfileType {
  name: string;
  title: string;
  avatar: string;
}
